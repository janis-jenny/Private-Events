<br>
<br>
<br>
<br>
<br>

<ul>
  <% @events.each do |e| %>
    <li><%= link_to e.title, event_path(e)%>
        <%= e.description %>
        <%= e.date %>
        <%= e.location %>
    </li>
  <% end %>
</ul>
